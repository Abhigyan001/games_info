<header>
  <nav class="gray-links">
    <ul class="flex space-between">
      <li class="logo">
        <%= image_tag("PC.png", :alt => "logo") %>
      </li>
      <ul class="flex">
        <li class= "orange-links"><%= link_to 'Home', root_path %></li>
        <li><%= link_to Category.find(53).name, category_path(Category.find(53)) %></li>
        <li><%= link_to Category.find(54).name, category_path(Category.find(54)) %></li>
        <li><%= link_to Category.find(55).name, category_path(Category.find(55)) %></li>
        <li><%= link_to Category.find(56).name, category_path(Category.find(56)) %></li>
      </ul>
      <ul class="flex">
        <%= session_controls %>
      </ul>
    </ul>
  </nav>
</header>
