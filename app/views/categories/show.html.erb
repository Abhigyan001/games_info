<div class="container grid">
  <h1>Hello There</h1>
  <% @recent_related_articles.each_with_index do |art, ind| %>
    <div class="grid article-image">
      <%= display_4articles(art, ind)%>
    </div>
  <% end %>
</div>